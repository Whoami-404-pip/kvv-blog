# Shared Images

Replace files in this folder to update the site artwork without touching content or theme files.

- `profile.png` is used for the author avatar
- `hero.png` is used in the sample shortcode and can be reused anywhere else
